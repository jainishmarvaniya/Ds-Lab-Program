#include<stdio.h>
#include<stdlib.h>
#include<time.h>
void main(){
	 int n,i,j,size;
	 
	 printf("Enter a size: ");
	 scanf("%d",&size);
	 
	   char a[size][size];
	 for(i=0;i<size;i++){
	 	for(j=0;j<size;j++){	 
	 	printf("Enter a word: ");
	 	scanf("%s",&a[i][j]);
	
	    }
	 }
	 
	 int random= rand()%size;
	 
	 
	 
	 
	 
	 
}
